import 'package:flutter/material.dart';

class BestSelingViewBody extends StatelessWidget {
  const BestSelingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("data"),
      ],
    );
  }
}
